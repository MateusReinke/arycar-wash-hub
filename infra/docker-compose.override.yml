services:
  backend:
    volumes:
      - ../backend:/app
      - /app/node_modules
    command: npm run start:dev

  frontend:
    volumes:
      - ../frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 3000
